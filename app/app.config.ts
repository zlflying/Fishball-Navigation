export default defineAppConfig({
    /** 图标配置 */
    icon: {
        /** 默认图标大小 */
        // size: '1em',
        /** 自定义图标类名 */
        class: 'nuxt-icon',
        /** 图标模式 svg | css */
        mode: 'css',
        /** 图标层 */
        cssLayer: 'base'
    }
})